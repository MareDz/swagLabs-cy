{
  "name": "swaglabs-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:login": "npx cypress run --spec 'cypress/tests/login.spec.ts'",
    "test:products": "npx cypress run --spec 'cypress/tests/products.spec.ts'",
    "test:cart": "npx cypress run --spec 'cypress/tests/shoppingCart.spec.ts'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress-xpath": "^2.0.1",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "cypress": "^13.6.5"
  }
}
